<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizandregistration">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Science"></activity>
        <activity android:name=".English" />
        <activity android:name=".Registration" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".Login" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DisplayActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".Maths" /> <!-- <activity android:name=".QuizPage" /> -->
        <activity android:name=".MainActivity" />
        <activity android:name=".LandingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".QuizPage"
            android:parentActivityName=".HomeActivity" />
    </application>

</manifest>